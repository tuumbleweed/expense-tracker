# Project Layout

Layout that can be used with golang.

We keep all of the boilerplates here.
Copy whole project when starting another project, then delete all parts you are not going to use.


## Installation
```bash
sudo apt install tesseract-ocr tesseract-ocr-spa
sudo apt install libtesseract-dev libleptonica-dev
```